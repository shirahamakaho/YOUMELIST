<div class="container">

  <div class="row justify-content-center">
    <div class="card">
      <div class="card-body">
        <%= image_tag @user.get_profile_image(200,200),class:"mt-3 rounded-circle"; %>
        <%= form_with model:@user,url:user_path(@user),method: :patch do |f| %>
        <%= f.label:プロフィール画像 %>
        <%= f.file_field :profile_image, accept: "image/*" %>
        <%= f.label:ユーザーネーム %><br>
        <%= f.text_field:name %><br>
        <%= f.label:プロフィール %><br>
        <%= f.text_area:introduction %><br>
        <%= f.submit "保存" %>
        <% end %>
        </div>
      </div>
    </div>

  <div class="row my-3 justify-content-between">
    <div class="col-4 offset-2">
      <h4>メールアドレス<i class="fas fa-envelope"></i></h4>
      <h4>イイねしたコメント</h4>
      <h4>ユメの数</h4>
      <h4>コメント数</h4>
    </div>
    <div class="col-6">
      <p><%= @user.email %></p>
      <p><%= @user.favorites.count %></p>
      <p><%= @user.dreams.count %></p>
      <p><%= @user.comments.count %></p>
      <%= @user.is_deleted %>
      <p>password_edit</p>
    </div>
  </div>
    <div class="row justify-content-center text-center">
      <div class="col-6">
        <p><%= link_to "ログアウト",destroy_user_session_path,method: :delete %></p>
        <p><%= link_to "退会する", withdrawal_path,method: :patch,data:{confilm:"本当に退会しますか？"} %></p>
        <p>退会すると、再度新規登録をする必要があります。</p>
      </div>
    </div>

</div>
